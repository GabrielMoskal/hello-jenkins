/* Requires the Docker Pipeline plugin  test2*/
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'set'
                bat 'echo Hello World'
            }
        }
    }
}